
spring:
  data:
    mongodb:
      uri: mongodb+srv://USERNAME:PWD@paf.eo9iuqw.mongodb.net/pafdb?retryWrites=true&w=majority&appName=PAF

 server:
  port: 8080

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://sadeesh:82539685%23Jmc@paf.eo9iuqw.mongodb.net/smartcampus_ops?retryWrites=true&w=majority}
  
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

app:
  apiBase: /api/v1
  jwt:
    issuer: smart-campus-ops-hub
    ttlMinutes: 240
    secret: "dev-only-change-this-secret-dev-only-change-this-secret"
  uploads:
    dir: "${user.home}/smartcampus-uploads"
    maxBytes: 5242880

logging:
  level:
    org.springframework.web: DEBUG
    com.smartcampus.ops: DEBUG

# CORS Configuration
cors:
  allowed-origins: http://localhost:3000,http://localhost:4200
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true