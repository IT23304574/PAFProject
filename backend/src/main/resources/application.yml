spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://sadeesh:82539685%23Jmc@paf.eo9iuqw.mongodb.net/smartcampus_ops?retryWrites=true&w=majority}

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

  # Silence the open-in-view warning (safe to keep even after removing JPA)
  jpa:
    open-in-view: false

server:
  port: 8080

app:
  apiBase: /api/v1
  jwt:
    issuer: smart-campus-ops-hub
    ttlMinutes: 240
    secret: "${JWT_SECRET:dev-only-change-this-secret-dev-only-change-this-secret}"
  uploads:
    dir: "${user.home}/smartcampus-uploads"
    maxBytes: 5242880

logging:
  level:
    org.springframework.web: DEBUG
    com.smartcampus.ops: DEBUG